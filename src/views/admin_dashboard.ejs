<!-- server/src/views/admin_dashboard.ejs -->
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <style>
        body { font-family: sans-serif; margin: 0; background-color: #f8f9fa; }
        .header { background-color: #343a40; color: white; padding: 1rem; display: flex; justify-content: space-between; align-items: center; }
        .header h1 { margin: 0; font-size: 1.5rem; }
        .header a { color: #ffc107; text-decoration: none; }
        .container { padding: 2rem; }
        .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1.5rem; }
        .stat-card { background: white; padding: 1.5rem; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); text-align: center; }
        .stat-card i { font-size: 2.5rem; margin-bottom: 1rem; } /* Placeholder for icons */
        .stat-card h3 { font-size: 2rem; margin: 0.5rem 0; color: #343a40; }
        .stat-card p { color: #6c757d; margin: 0; }
        /* Màu sắc cho icons */
        .icon-users { color: #007bff; }
        .icon-companies { color: #fd7e14; }
        .icon-jobs { color: #28a745; }
        .icon-apps { color: #6f42c1; }
    </style>
    <!-- Tạm thời dùng link CDN cho Font Awesome icons -->
     <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="header">
        <h1>Admin Dashboard</h1>
        <a href="/api/admin/logout">Đăng xuất</a> <!-- Sẽ tạo route này sau -->
    </div>

    <div class="container">
        <h2>Thống kê Hệ thống</h2>

        <% if (typeof stats !== 'undefined' && stats) { %>
            <div class="stats-grid">
                <a href="/api/admin/users-list" style="text-decoration: none;">
                <div class="stat-card">
                    <i class="fas fa-users icon-users"></i>
                    <h3><%= stats.totalUsers %></h3>
                    <p>Tổng Người Dùng</p>
                </div>
            </a>
                <a href="/api/admin/companies-list" style="text-decoration: none;">
                <div class="stat-card">
                    <i class="fas fa-building icon-companies"></i>
                    <h3><%= stats.totalCompanies %></h3>
                    <p>Tổng Công Ty</p>
                </div>
            </a>
                <a href="/api/admin/jobs-list" style="text-decoration: none;">
                <div class="stat-card">
                    <i class="fas fa-briefcase icon-jobs"></i>
                    <h3><%= stats.totalJobs %></h3>
                    <p>Tổng Việc Làm</p>
                </div>
            </a>
                <a href="/api/admin/applications-list" style="text-decoration: none;">
                <div class="stat-card">
                    <i class="fas fa-folder-open icon-apps"></i>
                    <h3><%= stats.totalApplications %></h3>
                    <p>Tổng Hồ Sơ</p>
                </div>
            </a>
            </div>
        <% } else { %>
            <p>Không thể tải dữ liệu thống kê.</p>
        <% } %>

        <!-- Thêm các phần quản lý khác ở đây (ví dụ: bảng danh sách) -->

    </div>
</body>
</html>